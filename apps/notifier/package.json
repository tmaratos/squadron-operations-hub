{
  "name": "@squadron/notifier",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "deploy": "node deploy.mjs",
    "tail": "node deploy.mjs tail squadron-notifier --format pretty",
    "versions": "node deploy.mjs deployments list --config wrangler.jsonc"
  }
}
